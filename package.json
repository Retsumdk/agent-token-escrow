{
  "name": "agent-token-escrow",
  "description": "Secure escrow system for agent-to-agent transactions, ensuring payment is only released upon verifiable proof of task completion",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}